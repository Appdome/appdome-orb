description: >
  In this example we are fusing an android app with auto signing.
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    appdome-orb: appdome/appdome-orb@x.y.z
  workflows:
    Appdome fusion:
      jobs:
        - appdome-orb/appdome_runner:
              runner: "ANDROID_AUTO_SIGNING"
              appFile: <path_to_apk_or_aab_file>
              output: <output_apk_or_aab>
              fusion-set-id: <Appdome_Fusion_Set_ID>
              google-play-signing: <true_or_false>
