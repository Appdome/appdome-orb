description: >
  In this example we are fusing an iOS app with private signing.
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    appdome-orb: appdome/appdome-build_2secure@x.y.z
  workflows:
    Appdome fusion:
      jobs:
        - appdome-orb/appdome_runner:
              runner: "IOS_AUTO_DEV_SIGNING"
              appFile: <path_to_ipa_file>
              output: <output_ipa>
              team-id: <your_team's_ID>
